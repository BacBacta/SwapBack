name: Deploy with Anchor v0.31.0 (Cargo.lock v3 for cargo-build-sbf)

on:
  workflow_dispatch:
    inputs:
      program_name:
        description: 'Programme à déployer'
        required: true
        default: 'swapback_cnft'
        type: choice
        options:
          - swapback_cnft
          - swapback_router

env:
  SOLANA_VERSION: v2.0.15
  ANCHOR_VERSION: v0.31.0

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust stable (Anchor v0.31.0 compatible)
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Verify Rust version
        run: |
          echo "✅ Using Rust stable (Anchor v0.31.0 supports Rust 1.77.0 - 1.83.0+)"
          rustc --version
          cargo --version

      - name: NO CACHE - Fresh build every time
        run: |
          echo "⚠️ Cache DISABLED for clean Cargo.lock v4 generation"
          echo "Modern Rust + Anchor support v4 natively"

      - name: NUCLEAR - Remove ALL Cargo.lock files
        run: |
          echo "🔥 Obliterating all Cargo.lock files..."
          find . -name "Cargo.lock" -type f -print -delete 2>/dev/null || true
          rm -f Cargo.lock 2>/dev/null || true
          git rm --cached -f Cargo.lock 2>/dev/null || true
          git rm --cached -f "**/Cargo.lock" 2>/dev/null || true
          
          REMAINING=$(find . -name "Cargo.lock" -type f 2>/dev/null | wc -l)
          if [ "$REMAINING" -gt 0 ]; then
            echo "❌ $REMAINING Cargo.lock files remain"
            exit 1
          fi
          echo "✅ All Cargo.lock obliterated"

      - name: Generate Cargo.lock (v4 native support)
        run: |
          echo "🔧 Generating Cargo.lock with modern toolchain..."
          echo "Rust: $(rustc --version)"
          echo "Cargo: $(cargo --version)"
          echo "Solana CLI: v2.0.x supports Cargo.lock v4 natively!"
          
          # Generate lockfile (v4 is fine with Solana 2.0+)
          cargo generate-lockfile
          
          echo ""
          echo "Generated Cargo.lock:"
          head -5 Cargo.lock
          
          LOCK_VERSION=$(head -5 Cargo.lock | grep "version = " | grep -oE '[0-9]+' || echo "unknown")
          echo ""
          echo "✅ Cargo.lock version $LOCK_VERSION (supported by Solana CLI 2.0+)"

      - name: Install Solana CLI (GitHub releases - reliable)
        run: |
          echo "📦 Installing Solana CLI ${{ env.SOLANA_VERSION }}..."
          
          # Use GitHub releases directly (more reliable than release.solana.com)
          VERSION_NUM="${{ env.SOLANA_VERSION }}"
          
          echo "Downloading from GitHub releases..."
          wget https://github.com/solana-labs/solana/releases/download/${VERSION_NUM}/solana-release-x86_64-unknown-linux-gnu.tar.bz2 -O solana.tar.bz2 || {
            echo "⚠️ ${VERSION_NUM} unavailable, trying v1.18.18..."
            wget https://github.com/solana-labs/solana/releases/download/v1.18.18/solana-release-x86_64-unknown-linux-gnu.tar.bz2 -O solana.tar.bz2 || {
              echo "❌ Download failed"
              exit 1
            }
          }
          
          echo "Extracting..."
          tar xjf solana.tar.bz2
          
          echo "Installing to ~/.local/share/solana..."
          mkdir -p $HOME/.local/share/solana/install/active_release
          cp -r solana-release/* $HOME/.local/share/solana/install/active_release/
          
          echo "Cleaning up..."
          rm -rf solana-release solana.tar.bz2
          
          echo "Adding to PATH..."
          echo "$HOME/.local/share/solana/install/active_release/bin" >> $GITHUB_PATH
          export PATH="$HOME/.local/share/solana/install/active_release/bin:$PATH"
          
          echo "Verifying installation..."
          solana --version
          solana-keygen --version
          echo "✅ Solana CLI installed successfully"

      - name: Install Anchor CLI v0.31.0 (modern, v4 compatible)
        run: |
          echo "📦 Installing Anchor CLI ${{ env.ANCHOR_VERSION }}..."
          
          # Install directly from git tag
          cargo install --git https://github.com/coral-xyz/anchor \
            --tag ${{ env.ANCHOR_VERSION }} \
            anchor-cli \
            --locked \
            --force
          
          anchor --version
          echo "✅ Anchor CLI ${{ env.ANCHOR_VERSION }} installed (Rust stable + Cargo.lock v4 compatible)"

      - name: Configure Solana
        run: |
          solana config set --url devnet
          echo "${{ secrets.DEPLOYER_PRIVATE_KEY }}" > deployer-keypair.json
          solana config set --keypair deployer-keypair.json
          
          PUBKEY=$(solana address)
          echo "💼 Deployer: $PUBKEY"
          
          BALANCE=$(solana balance --url devnet | grep -oE '[0-9]+\.[0-9]+' | head -n1 || echo "0")
          echo "💰 Balance: $BALANCE SOL"
          
          if (( $(echo "$BALANCE < 1.0" | bc -l) )); then
            echo "⚠️ Low balance, requesting airdrop..."
            solana airdrop 2 --url devnet || echo "Airdrop failed, continuing..."
          fi

      - name: Verify declare_id!
        run: |
          PROGRAM_ID="${{ github.event.inputs.program_name == 'swapback_cnft' && '26kzow1KF3AbrbFA7M3WxXVCtcMRgzMXkAKtVYDDt6Ru' || 'opPhGcth2dGQQ7njYmkAYwfxspJ1DjgP9LV2y1jygCx' }}"
          
          CURRENT_ID=$(grep -A1 "declare_id!" programs/${{ github.event.inputs.program_name }}/src/lib.rs | grep -oE '[A-Za-z0-9]{32,}' | head -1)
          
          if [ "$CURRENT_ID" != "$PROGRAM_ID" ]; then
            echo "❌ declare_id! mismatch!"
            echo "Expected: $PROGRAM_ID"
            echo "Found: $CURRENT_ID"
            exit 1
          fi
          
          echo "✅ declare_id! correct: $CURRENT_ID"

      - name: Pre-build verification
        run: |
          echo "🔍 Pre-build check..."
          
          if [ ! -f "Cargo.lock" ]; then
            echo "❌ Cargo.lock not found!"
            exit 1
          fi
          
          echo "✅ Cargo.lock ready:"
          head -5 Cargo.lock
          
          echo ""
          echo "📦 Ready to build with Solana CLI 2.0 (Cargo.lock v4 native support)"

      - name: Build with Anchor
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🔨 BUILDING: ${{ github.event.inputs.program_name }}"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          
          # Build with Anchor
          set +e
          anchor build --program-name ${{ github.event.inputs.program_name }} 2>&1 | tee build.log
          BUILD_EXIT_CODE=${PIPESTATUS[0]}
          set -e
          
          # Check for Cargo.lock errors (should not happen with Solana 2.0)
          if grep -q "lock file version" build.log; then
            echo "❌ CARGO.LOCK VERSION ERROR"
            grep -A10 -B5 "lock file version" build.log
            exit 1
          fi
          
          if [ $BUILD_EXIT_CODE -ne 0 ]; then
            echo "❌ BUILD FAILED (Exit code: $BUILD_EXIT_CODE)"
            tail -200 build.log
            exit $BUILD_EXIT_CODE
          fi
          
          echo "✅ BUILD SUCCESSFUL"

      - name: Verify build artifacts
        run: |
          SO_FILE="target/deploy/${{ github.event.inputs.program_name }}.so"
          KEYPAIR_FILE="target/deploy/${{ github.event.inputs.program_name }}-keypair.json"
          
          if [ ! -f "$SO_FILE" ]; then
            echo "❌ Binary not found: $SO_FILE"
            exit 1
          fi
          
          if [ ! -f "$KEYPAIR_FILE" ]; then
            echo "❌ Keypair not found: $KEYPAIR_FILE"
            exit 1
          fi
          
          echo "✅ Binary: $SO_FILE ($(du -h $SO_FILE | cut -f1))"
          echo "✅ Keypair: $KEYPAIR_FILE"
          
          PROGRAM_ID=$(solana-keygen pubkey $KEYPAIR_FILE)
          echo "📌 Program ID: $PROGRAM_ID"

      - name: Deploy to devnet
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🚀 DEPLOYING: ${{ github.event.inputs.program_name }}"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          
          PROGRAM_ID="${{ github.event.inputs.program_name == 'swapback_cnft' && '26kzow1KF3AbrbFA7M3WxXVCtcMRgzMXkAKtVYDDt6Ru' || 'opPhGcth2dGQQ7njYmkAYwfxspJ1DjgP9LV2y1jygCx' }}"
          
          solana program deploy \
            target/deploy/${{ github.event.inputs.program_name }}.so \
            --program-id target/deploy/${{ github.event.inputs.program_name }}-keypair.json \
            --url devnet \
            --upgrade-authority deployer-keypair.json \
            --max-retries 10 \
            --use-rpc \
            2>&1 | tee deploy.log
          
          if grep -q "successfully" deploy.log; then
            echo "✅ DEPLOYMENT SUCCESSFUL"
          else
            echo "❌ DEPLOYMENT FAILED"
            cat deploy.log
            exit 1
          fi

      - name: Verify deployment
        run: |
          PROGRAM_ID="${{ github.event.inputs.program_name == 'swapback_cnft' && '26kzow1KF3AbrbFA7M3WxXVCtcMRgzMXkAKtVYDDt6Ru' || 'opPhGcth2dGQQ7njYmkAYwfxspJ1DjgP9LV2y1jygCx' }}"
          
          echo "🔍 Verifying deployment..."
          solana program show $PROGRAM_ID --url devnet
          
          echo ""
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "✅ DEPLOYMENT COMPLETE"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Program ID: $PROGRAM_ID"
          echo "Network: Devnet"
          echo ""
          echo "Test on Vercel:"
          echo "https://swap-back-pc5qkn6em-bactas-projects.vercel.app/"
