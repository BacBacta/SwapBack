const http = require("http"); http.get("http://localhost:3000", (res) => { console.log("Status:", res.statusCode); let data=""; res.on("data", chunk => data += chunk); res.on("end", () => { if (data.includes("<html>") || data.includes("<!DOCTYPE")) { console.log("✅ Application fonctionne"); } else { console.log("❌ Problème détecté"); console.log("Réponse:", data.substring(0, 100)); } }); }).on("error", (err) => { console.log("❌ Erreur:", err.message); }); setTimeout(() => process.exit(1), 5000);
